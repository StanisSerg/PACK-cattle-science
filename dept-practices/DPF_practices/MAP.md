---
type: visual-map
created: 2026-08-09
wp: WP-109
updated: 2026-08-09 (шаг 03 на приёмке)
---

# Карта работы FPF — где мы находимся

> Открывать в VS Code (preview) или на GitHub — схема рендерится.
> Обновлять при переходе шага: переставить маркер `YOU` и классы статусов.

```mermaid
flowchart TD
    subgraph COURSE["WP-109 · Курс FPF (5 семинаров) — семинары 1–4 ✅, семинар 5 — сегодня"]
        direction TB

        subgraph L1["СЛОЙ 1 · Применение метода (full-cycle, кейс «цикл визита», метод B)"]
            direction LR
            S0["0 · Вход: зацепка"] --> S1["1 · Карточка проблемы"] --> S2["2 · Граница системы"] --> S3["3 · Межуровневый конфликт"] --> S4["4 · Выбор метода"] --> S5["5 · Method claim"] --> S6["6 · P2W-развёртка"] --> S7["7 · Потоки"] --> S8["8 · Язык и публикация"] --> S9["9 · Пилот + измерение"] --> S10["10 · Приёмка"]
        end

        subgraph L2["СЛОЙ 2 · Создание DPF/LPF (DPF_practices, маршрут семинара 5)"]
            direction TB
            D00["00 · Гейт нужности"] --> D02["02 · Корпус случаев<br/>4 механизма разрыва"] --> D03["03 · SoTA<br/>4 линии источников"]
            D03 --> D04["04 · Intake → кандидаты → DRR<br/>✅ выбран B, DRR admissible"]
            D04 --> D05["05 · Первый паттерн<br/>◄◄◄ МЫ ЗДЕСЬ (следующий шаг)"]
            D04 --> D05["05 · Первый паттерн"] --> D06["06 · Тело паттерна E.8"] --> D07["07 · Пакет: README/ToC"] --> D08["08 · Применение к новому случаю"] --> D09["09 · Местный LPF<br/>(сюда: метод B, D1–D4, чек-листы)"] --> D10["10 · Оценка/ремонт/повтор"] --> D11["11 · Контур и агенты"] --> D12["12 · Приёмка мини-DPF/LPF"]

            subgraph DRRLoop["DRR-контур внутри шага 04"]
                direction LR
                E9["E.9 · записать решение"] --> E9DA["E.9.DA · ревьюер (чистый контекст)"]
                E9DA -->|"находки"| E23["E.23 · починка"] --> E9DA
                E9DA -->|"admissible"| ACC["Принятие пилотом<br/>(записано ≠ принято)"]
            end
            D04 -.-> DRRLoop
        end
    end

    L1 ==>|"метод B готов ◐<br/>ждёт предметных решений"| L2

    classDef done fill:#c8e6c9,stroke:#2e7d32,color:#000
    classDef review fill:#fff9c4,stroke:#f9a825,color:#000
    classDef current fill:#ffcdd2,stroke:#c62828,stroke-width:3px,color:#000
    classDef todo fill:#eceff1,stroke:#90a4ae,color:#000

    class S0,S1,S2,S3,S4,S5,S6,S7,S8 done
    class S9,S10 todo
    class D00,D02,D03,D04 done
    class D05 current
    class D06,D07,D08,D09,D10,D11,D12 todo
```

## Легенда

| Цвет | Значение |
|---|---|
| 🟩 зелёный | шаг пройден и принят |
| 🟨 жёлтый | на приёмке у пилота |
| 🟥 красный | **текущая позиция** |
| ⬜ серый | впереди |

## Текущее состояние одним взглядом

- **Слой 1 (применение):** шаги 0–8 пройдены, все ◐ на приёмке; метод B готов, ждёт твои предметные решения (список допуска D1, форма сигнала, эскалация) → потом шаг 9 (пилот, замер базовой линии).
- **Слой 2 (создание DPF):** гейт пройден, корпус из 4 случаев принят, SoTA из 4 линий — **на твоей приёмке** (`03-sota.md`).
- **Следующий шаг — 04:** самый важный в маршруте: кандидаты архитектуры DPF → **ты выбираешь** → агент записывает DRR → независимый ревьюер проверяет по E.9.DA → петля починки → твоё принятие.

## Куда что ложится (напоминание)

- **DPF** (переносимое ядро): различения «4 механизма разрыва цикла визита», способ распознавания, граница near-miss «ждали анализы кормов».
- **LPF отдела** (местная практика): метод B, список допуска D1, форма сигнала, чек-листы ролей (будущий РП — решение на Strategy Session W33).
- **DRR:** нужен только на шаге 04 слоя 2 (авторинг); в слое 1 не нужен («инструмент автора, не пользователя»).

---

*Обновляется вручную при смене шага. Файлы шагов — в этой же папке (`NN-*.md`), гайды — `docs/fpf/FPF_practical/`.*
